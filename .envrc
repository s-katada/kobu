# Full kobu devshell (firmware + web). Requires nix-direnv.
# Sub-directories `v1/firmware/` and `v1/web/` have their own `.envrc`
# that opt into lighter shell variants (firmware-only / web-only),
# overriding this one when you `cd` into them.
use flake
